# MethodQuad

Group: Sections

The four principles that hold on every Capita engagement ("How we engage"). Distinct from the Services (scoped, priced engagements) and from the Growth Method (how the work itself gets done). Source: the `<section id="method">` block in `src/pages/index.astro`, expanded from the `methodBlocks` array (4 items).

## Structure

- `.section-head` with an `h2.display` ("How we engage.") and a `.lede.gm-bridge` that links onward to the Growth Method. The bridge `<a>` is kept (href neutralised to `#`).
- `.method-quad` of four `.method-cell` articles, each with an `.eyebrow` tag, an `<h3>`, and a body `<p>`.

All copy is verbatim from the source array and section head.
