# Outcomes

Group: Sections

The three outcomes Capita is hired to move, framed proof-first. Source: the `<section id="outcomes">` block in `src/pages/index.astro`, expanded from the `outcomes` array (3 items: AI, Retention, Productivity).

## Structure

- `.section-head` with a `.section-label` ("Outcomes"), an `h2.display` headline, and a `.lede`.
- `.outcome-grid` of three `.outcome-col` articles, each with an `.outcome-label` (the short category tag), an `<h3>`, and a body `<p>`.

All copy is verbatim from the source array and section head.
